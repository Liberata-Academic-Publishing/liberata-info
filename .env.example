# Copy to .env.local and fill in. Vite only exposes VITE_-prefixed variables
# to client code, so these names matter. CI passes the same values from the
# repository secrets (still named REACT_APP_SUPABASE_*).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
