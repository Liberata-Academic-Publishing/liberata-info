# Dummy values so the Supabase client can be constructed when the test suite
# imports it. Not secrets; the real values come from CI secrets at build time.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
